package il.co.nolife.fist;

public enum StandardPicture {

	// All standard icons will have a representation here, that way other objects requiring
	// these will not be passing the entire icons but this enum instead
	
}
